'use client';import type {Profile} from '@/lib/types';
export default function ProfileSidebar({profiles,activeId,setId,create,del}:{profiles:Profile[];activeId?:string;setId:(x:string)=>void;create:()=>void;del:(x:string)=>void}){return <aside className="pane flex flex-col"><div className="section-title m-3 mb-0">Company Profiles</div><div className="scroll flex-1">{profiles.map(p=><button key={p.id} onClick={()=>setId(p.id)} className={`list-row w-full text-left border-y-0 border-r-0 bg-transparent ${activeId===p.id?'active':''}`}><b className="text-[12px] block pr-7 truncate">{p.name}</b>{p.isDefault&&<span className="badge inline-block mt-2">Default</span>}<span role="button" className="hover-x text-[var(--danger)]" onClick={e=>{e.stopPropagation();del(p.id)}}>×</span></button>)}</div><button className="btn m-3" onClick={create}>+ New Profile</button></aside>}
