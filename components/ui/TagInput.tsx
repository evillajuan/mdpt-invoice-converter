'use client';import {useState} from 'react';
export default function TagInput({values,onChange,onError}:{values:string[];onChange:(x:string[])=>void;onError:(x:string)=>void}){const [draft,setDraft]=useState('');const commit=()=>{const v=draft.trim().replace(/[,;]$/,'');if(!v)return;if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v)){onError('Enter a valid email address');return}if(!values.includes(v))onChange([...values,v]);setDraft('')};return <div className="min-h-[38px] border border-[var(--rule2)] bg-white flex flex-wrap gap-1 p-1.5">{values.map(v=><span key={v} className="bg-[var(--stone)] border border-[var(--rule)] px-2 py-1 text-[11px]">{v}<button onClick={()=>onChange(values.filter(x=>x!==v))} className="ml-2 border-0 bg-transparent">×</button></span>)}<input aria-label="Email address" className="border-0 p-1 min-w-[120px] flex-1" value={draft} onChange={e=>setDraft(e.target.value)} onBlur={commit} onKeyDown={e=>{if(['Enter',',',';','Tab'].includes(e.key)){e.preventDefault();commit()}if(e.key==='Backspace'&&!draft&&values.length)onChange(values.slice(0,-1))}}/></div>}
